# Keep worker scripts LF so Windows CRLF doesn't corrupt the dispatcher.
# (jq CRLF output is handled separately in call_worker.sh via `tr -d '\r'`.)
*.sh   text eol=lf
*.py   text eol=lf
_shared/backends.json text eol=lf
*.toml text eol=lf
*.md   text eol=lf
